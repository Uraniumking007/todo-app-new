import './style.css'
import "@lottiefiles/lottie-player";
import { animatedImage, todoList } from "./var";
import {getAllTodos }from '../api/db-functions/getallTodos'

// if (getAllTodos != null) {
  // todoList.innerHTML = `${getAllTodos}`
// } else {
  // console.log('nothing')
// }